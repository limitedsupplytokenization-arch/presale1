{
  "name": "lst-presale-contract",
  "version": "1.0.0",
  "description": "LST Token Presale Smart Contract",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run deploy.js --network base",
    "deploy:testnet": "hardhat run deploy.js --network baseGoerli",
    "verify:base": "hardhat verify --network base",
    "verify:testnet": "hardhat verify --network baseGoerli",
    "test": "hardhat test"
  },
  "keywords": [
    "solidity",
    "ethereum",
    "base",
    "presale",
    "lst",
    "token"
  ],
  "author": "LST Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "web3": "^1.8.0"
  }
}
